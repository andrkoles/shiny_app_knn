## Shiny App and _k_-NN Model

A Python Shiny app with number of neighbors as a primary input.

The output is a confusion matrix with the predictions of a _k_-NN

model. The _k_-NN model is trained on the `scikit-learn` breast

cancer dataset.
